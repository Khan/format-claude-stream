# adapters

This directory contains classes that connect the `src/core` code to the
outside world. The adapter classes implement interfaces in `src/core/ports`.

See [hexagonal architecture, ports and adapters].

[hexagonal architecture, ports and adapters]: https://alistair.cockburn.us/hexagonal-architecture
